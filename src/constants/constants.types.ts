export interface CONST_GAME {
  INITIAL_STAGE: number;
  INITIAL_SCORE: number;
  INITIAL_LEFTTIME: number;
  MIN_LEFTTIME: number;
  DEDUCT_LEFTTIME: number;
  DEDUCT_LEFTTIME_WRONG_ANSWER: number;
}
